# from database.ext import db

# class User(db.Model):
#     __tablename__ = 'user'
#     id = db.Column(db.INTEGER, primary_key=True)
#     username = db.Column(db.String(45), unique=True)
#     password = db.Column(db.String(45), nullable=True)
#     types = db.Column(db.INTEGER, nullable=True)
